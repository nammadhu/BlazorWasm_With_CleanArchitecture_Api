namespace BlazorWasmPwaClient
    {
    public static class DependencyInjectionPwa
        {
        //currently not using, but if more required then can be added here which are specific to pwa only
        public static IServiceCollection AddDependencyInjectionPwa(this IServiceCollection services)
            {

            return services;
            }
        }
    }
